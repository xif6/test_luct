import { ProductsTypePipe } from './products-type.pipe';

describe('ProductsTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductsTypePipe();
    expect(pipe).toBeTruthy();
  });
});
